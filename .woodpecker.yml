---
steps:
  - name: hello
    image: alpine:latest
    environment:
      GREETING: "Hello from Woodpecker 3.x!"
    commands:
      - echo "${GREETING}"
      - echo "Repository: ${CI_REPO}"
      - echo "Branch: ${CI_COMMIT_BRANCH}"
      - echo "Commit: ${CI_COMMIT_SHA}"
